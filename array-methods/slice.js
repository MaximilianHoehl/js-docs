const arr = [1,2,4,6,8,6,4];

//slice         -> slice = schnitt; returns sliced array from start to end index. Start is included, end not
const res = arr.slice(2, 6);
console.log(res); //_> [ 4, 6, 8, 6 ]